# Build products
build/
app/build/
.gradle/
local.properties

# Synced web assets (generated by sync-assets.sh from ../extension)
app/src/main/assets/

# Signing secrets — back these up OUTSIDE the repo; never commit them
keystore/

# IDE
.idea/
*.iml
